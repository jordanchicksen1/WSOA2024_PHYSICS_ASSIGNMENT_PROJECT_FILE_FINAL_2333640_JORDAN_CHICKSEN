using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class detection : MonoBehaviour
{
    //Rigidbody2D rb;
    //bool player;

    //float playerScore = 0f;
    //float comScore = 0f;

    //int playerTouches = 0;
    //int comTouches = 0;





    //private void OnCollisionEnter2D(Collision2D collision)
    //{
    //    if (collision.transform.tag == "Player")
    //    {
    //        player = true;

    //        playerTouches++;
    //        if (playerTouches > 1 && playerScore < 0)
    //        {
    //            playerScore--;
    //            playerTouches = 0;

    //        }

    //    }
    //    else if (collision.transform.tag == "COM")
    //    {
    //        player = false;

    //        comTouches++;
    //        if (comTouches > 1 && playerScore < 0)
    //        {
    //            comScore--;
    //            comTouches = 0;

    //        }
    //    }
    }
